<!--form_for([@post, @comment]) do |f| %>-->
    <!-- f.text_area :body %>-->
    <!-- f.submit %>-->
<!--end %>-->


<%= render 'form', comment: @comment %><br>
<tr>
  <nav>
     <td><%= link_to t('Back'), posts_path %></td>
  </nav>
</tr>

