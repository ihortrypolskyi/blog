$('#comments_list').fadeOut(500, function(){
    $(this).remove();
    $("#post-comments_<%= @post.id %>").append("<%= j render partial: 'posts/comments_list',
     locals: {comment: @comment} %>");
});