<nav class="navbar navbar-fixed-top navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
              data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a id="image"><%= image_tag('logog.png', width: '120', id: 'logo') %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-left">
        <li><%= link_to 'En', en_path, locale: 'en' %></li>
        <li><%= link_to t('Ru'), ru_path, locale: 'ru' %></li>
      </ul>
      <ul class="nav navbar-nav" class="text-center">
        <li><a><%= render partial: 'shared/flash_messages', flash: flash %></a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
            <ul class="nav navbar-nav">
              <li><%= link_to t('All posts'), '/' %></li>
              <li><%= link_to t('Log out'), logout_path, method: 'delete' %></li>
              <li><a><%= current_user.first_name %> <%= current_user.last_name %></a></li>
            </ul>
        <% else %>
            <ul class="nav navbar-nav">
              <li><%= link_to t('Login'), 'login' %></li>
              <li><%= link_to t('Sign up'), 'signup' %>  </li>
            </ul>
        <% end %>
      </ul>
    </div>
  </div>
</nav>